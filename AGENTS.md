# 项目升级与 GitHub 同步

- 用户已授权本项目后续每次优化、修复或升级完成后自动提交并推送到现有 GitHub 仓库，无需再次询问是否同步。
- 发布前运行与改动相关的测试、构建及 `scripts/check_public_source.py`，检查差异；仅提交本次任务的文件，不夹带其他未提交改动。
- 推送后核对远端分支与本地提交一致，并检查该提交对应的 GitHub Actions 结果。失败时先诊断修复；无法完成时明确报告阻塞，不把本地完成说成已同步。
- 不提交账号凭证、个人资料、运行数据、图片、日志、备份、虚拟环境或构建产物；保留现有加密配置、历史任务及本地 API 密钥。
- 不强推、不覆盖远端其他人的提交；出现冲突时先核对，无法安全合并再说明具体问题。
- 最终回复简述是否同步成功及提交号。此规则仅适用于本项目，不代表授权修改其他仓库或部署外部服务。
